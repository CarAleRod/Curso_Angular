import { Header20Directive } from './header20.directive';

describe('Header20Directive', () => {
  it('should create an instance', () => {
    const directive = new Header20Directive();
    expect(directive).toBeTruthy();
  });
});
